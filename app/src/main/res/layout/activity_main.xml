<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".MainActivity">
    <TextView
        android:layout_width="match_parent"
        android:layout_height="20dp"
        android:text="要下载资源的URL"/>
    <EditText
        android:id="@+id/targetURL"
        android:layout_width="match_parent"
        android:layout_height="300dp" />
    <Button
        android:layout_marginTop="20dp"
        android:id="@+id/button_download"
        android:layout_below="@+id/targetURL"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="开始下载"/>
    <Button
        android:layout_marginTop="20dp"
        android:id="@+id/button_download_stop"
        android:layout_below="@+id/button_download"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:text="暂停下载"/>
    <ProgressBar
        android:id="@+id/progressBar_now"
        style="@style/Widget.AppCompat.ProgressBar.Horizontal"
        android:layout_marginTop="10dp"
        android:layout_below="@+id/button_download_stop"
        android:layout_width="match_parent"
        android:layout_height="30dp" />
</RelativeLayout>
